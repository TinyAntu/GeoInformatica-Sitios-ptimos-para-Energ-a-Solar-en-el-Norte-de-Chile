# Dependencias reales del pipeline (Python 3.10+).
# `glob` y `os` son parte de la librería estándar (no van aquí) y `torch` no se usa.
# Antes de la entrega, fija las versiones exactas de TU entorno (lo pide la rúbrica):
#     pip freeze > requirements.txt      # o añade ==X.Y.Z a cada línea
numpy
pandas
geopandas
shapely
rasterio
scipy
scikit-learn
optuna
joblib
PyYAML
matplotlib
shap==0.52.0
surtgis
# Visor web (T7). Para el deploy en la nube basta app/requirements.txt (solo estas 3).
streamlit==1.61.1
folium==0.20.0
streamlit-folium==0.27.4
Pillow
