# Dependencias mínimas del visor web (T7) para el deploy en Streamlit Community Cloud.
# El visor solo lee los assets pre-generados en app/assets/ (PNG + JSON), así que NO
# necesita rasterio/geopandas/shap/surtgis: eso mantiene el build de la nube liviano.
streamlit==1.61.1
folium==0.20.0
streamlit-folium==0.27.4
